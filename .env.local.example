DATABASE_URL="file:./prisma/dev.db"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Your email becomes admin automatically on signup
ADMIN_EMAIL="your@email.com"

# Optional - only needed for email notifications
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
BREVO_SENDER_NAME=DrivenByFaith3D

# Optional - only needed for shipping labels
SHIPPO_API_KEY=

# Required for file uploads — enable Vercel Blob in your Vercel project dashboard
# (Storage tab → Create Blob Store) then copy the token here
BLOB_READ_WRITE_TOKEN=
